import React from 'react';

const FooterSectionHeader = ({ name }) => {
    return (
        <div className='text-lg text-muted-coral font-bold'>
            {name}
        </div>
    );
}

export default FooterSectionHeader;